# Generated by make_fixtures.py at smoke-test runtime. state_5.sqlite embeds
# host-absolute rollout paths, so it must never be committed.
fixtures/home/.codex/state_5.sqlite
fixtures/home/.local/share/opencode/opencode.db

# The fixtures model a fresh install, so these must stay absent. Their presence
# means a run leaked container writes onto the host, which smoke.sh asserts
# against. Never commit them.
fixtures/home/**/add_stat/
